@{
  Layout = "_Layout";
}

@model ToDoList.Models.Item

<h2>Are you sure you want to delete this?</h2>

@Html.DisplayNameFor(model => model.Description): @Html.DisplayFor(model => model.Description)
@using (Html.BeginForm()) // BeginForm() automatically creates a POST request to an action method whose name matches the .cshtml file it's contained in. That's why we need ActionName("Delete") in controller.cs. lesson 28 for more detail
{
    <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to List", "Index")